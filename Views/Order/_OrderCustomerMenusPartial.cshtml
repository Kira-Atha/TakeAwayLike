@model ASP_PROJECT.Models.POCO.Menu
@using Microsoft.AspNetCore.Http

<tr>
    <td>@Model.Name</td>
    <td>@Model.Price €</td>

    <td><a class="btn btn-outline-danger btn-sm" asp-controller="Order" asp-action="DeleteMenuOrdered" asp-route-menuId="@Model.Id">Supprimer du panier</a></td>
    
</tr>